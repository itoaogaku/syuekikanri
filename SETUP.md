# セットアップ手順（APIキー取得〜スプレッドシート接続）

初めての方向けに、順番どおり進めれば動くように細かく書いています。
所要時間は 30〜40 分ほどです。

---

## 全体の流れ

1. スプレッドシートを作り、プログラム（コード）を貼り付ける
2. Stripe の APIキーを取得する
3. Komoju の APIキーを取得する
4. スプレッドシートにキーと設定を入れる
5. サンプルで動作確認 → 実データを取得

---

## STEP 1. スプレッドシートにコードを入れる

### 1-1. スプレッドシートを新規作成
1. [Google スプレッドシート](https://sheets.google.com) を開く
2. 「空白のスプレッドシート」を新規作成
3. 名前を「売上レポート」などに変更（左上のタイトル部分）

### 1-2. Apps Script（プログラムを書く場所）を開く
1. メニューの **「拡張機能」→「Apps Script」** をクリック
2. 新しいタブでコード編集画面が開きます

### 1-3. タイムゾーンを日本に設定
1. 左側の歯車アイコン **「プロジェクトの設定」** をクリック
2. **タイムゾーン** を **「(GMT+09:00) 東京」** に設定
3. （任意）「`appsscript.json` マニフェスト ファイルをエディタで表示する」に **チェック** を入れておくと後で楽です

### 1-4. コードファイルを作って貼り付ける
このリポジトリの `src/` フォルダにある **10個のファイル**を、そのまま貼り付けます。

最初からある `コード.gs`（または `Code.gs`）を使い、残りは「＋」ボタンで追加します。

1. 左の「ファイル」欄の **「＋」→「スクリプト」** で新しいファイルを作成
2. ファイル名を入力（下の表の名前。`.gs` は自動で付くので不要）
3. `src/` の同名ファイルの中身を全部コピーして貼り付け
4. これを全ファイル分くり返す

| 作るファイル名 | 貼り付ける元 |
|---|---|
| `Main` | `src/Main.gs` |
| `Config` | `src/Config.gs` |
| `Business` | `src/Business.gs` |
| `Ledger` | `src/Ledger.gs` |
| `Stripe` | `src/Stripe.gs` |
| `Komoju` | `src/Komoju.gs` |
| `Aggregate` | `src/Aggregate.gs` |
| `Report` | `src/Report.gs` |
| `Http` | `src/Http.gs` |
| `Sample` | `src/Sample.gs` |

> 最初からある `コード.gs` は、名前を `Main` に変えて `src/Main.gs` を貼るか、
> 中身を空にして別途 `Main` を作ってもOKです。**ファイルの順番は関係ありません。**

5. 上部の **フロッピー（保存）アイコン** で保存（`Ctrl+S` / `⌘+S`）

### 1-5. メニューを表示させる
1. **スプレッドシートのタブに戻り、ページを再読み込み（更新）** します
2. 数秒後、メニューバーに **「売上レポート」** が追加されます
   - 出ない場合は、もう一度リロード、または Apps Script 画面で関数 `onOpen` を一度実行

### 1-6. サンプルで動作確認（キー不要）
1. メニュー **「売上レポート」→「★ サンプルデータで表示を確認」**
2. **初回は承認画面が出ます**（下の「⚠ 初回の承認について」を参照）
3. 承認後、`年度サマリー` `事業別サマリー` などのシートが自動で作られれば成功です

#### ⚠ 初回の承認について（重要）
自分で作ったスクリプトのため、Google が確認前アプリとして警告を出します。**安全です。**
1. 「承認が必要です」→ **自分のGoogleアカウントを選択**
2. 「このアプリはGoogleで確認されていません」→ 左下の **「詳細」** をクリック
3. **「（プロジェクト名）に移動（安全ではないページ）」** をクリック
4. アクセス内容を確認して **「許可」**

---

## STEP 2. Stripe の APIキーを取得

売上・入金データを読み取るためのキーです。**安全のため「読み取り専用の制限付きキー」** を作ります。

1. [Stripe ダッシュボード](https://dashboard.stripe.com) にログイン
2. **本番データを使う場合**、画面の「テスト環境」トグルが **オフ（本番）** になっていることを確認
3. 右上「開発者」メニュー（または「設定」）→ **「APIキー」** を開く
4. **「制限付きキーを作成」** をクリック
5. キー名を「売上レポート用（読み取り）」などにする
6. 権限を次のように設定（**すべて「読み取り」**、他は「なし」でOK）
   - **Payouts（支払い）** → 読み取り
   - **Balance transactions（残高取引）** → 読み取り
   - **Charges（支払い）** → 読み取り
7. 「キーを作成」→ 表示された **`rk_live_...` で始まるキーをコピー**
   - このキーは一度しか表示されないことがあるので、確実にコピーしてください

> 💡 制限付きキー（`rk_`）でも通常キー（`sk_`）でも動きますが、安全性の高い **制限付きキー** を推奨します。

---

## STEP 3. Komoju の APIキーを取得

1. [Komoju 管理画面](https://komoju.com/ja) にログイン
2. 対象の **店舗（ショップ）を選択**
3. **「設定」**（店舗設定）の中の **「APIキー」／「シークレットキー」** を探す
4. **「シークレットキー（Secret Key）」をコピー**
   - 「公開鍵（Publishable Key）」ではなく **シークレットキー** の方です
5. あわせて、Komoju の **決済手数料率**（例：3.65% など、契約内容による）を控えておく
   - 純額の概算に使います。分からなければ空でも動きます（後から設定可）

---

## STEP 4. スプレッドシートにキーと設定を入れる

1. スプレッドシートのメニュー **「売上レポート」→「① APIキー・年度開始月を設定」**
2. 順番に入力ダイアログが出るので、それぞれ入力：
   - **Stripe**：STEP 2 でコピーした `rk_live_...`
   - **Komoju**：STEP 3 でコピーしたシークレットキー
   - **Komoju 手数料率**：例 `0.0365`（3.65%の場合）。不明なら空でOK
   - **年度の開始月**：`4`（4月始まり）。暦年で管理したいなら `1`
   - 変更しない項目は **空欄のまま OK** を押せばスキップされます
3. 「設定を保存しました」と出れば完了

> 🔒 キーはコード内ではなく、Google の安全な保管領域（スクリプトプロパティ）に保存されます。

### 事業マッピングの設定
1. メニュー **「② 事業マッピングを編集」**
2. 開いた「事業マッピング」シートに、**キーワード → 事業名** を入力
   （例：`講座` → `オンライン講座事業`）
3. 商品名や注文名に含まれる言葉をキーワードにします

---

## STEP 5. 実データを取得

1. まず1か月分でテスト：メニュー **「④ 月を指定して取得」** → 例 `2026-06`
2. 「◯◯ 分を反映しました」と表示され、各シートに実データが入ります
3. **`入金照合` シートで「✓ 一致」** になっていれば、データが正しく取れています
   - 「要確認」が出た場合はご連絡ください（手数料・返金の扱いを調整します）
4. 過去分をまとめて入れたい場合は、月を変えて「④」を必要な月数ぶんくり返します
5. 毎月自動で取得したい場合は **「⑥ 毎月の自動取得をON」**
   - 毎月5日の朝に前月分を自動で取り込みます

---

## うまくいかないときは

| 症状 | 対処 |
|---|---|
| 「売上レポート」メニューが出ない | スプレッドシートをリロード。それでも出なければ Apps Script で `onOpen` を実行 |
| 承認画面で先に進めない | 「詳細」→「安全でないページに移動」→「許可」（STEP 1-6 参照） |
| `HTTP 401` エラー | APIキーが間違っている／権限不足。STEP 2〜4 を再確認 |
| `HTTP 403` エラー | Stripe 制限付きキーの権限（Payouts / Balance transactions / Charges の読み取り）を確認 |
| 商品名が「(商品名なし)」ばかり | Wix が決済側に商品名を渡していない可能性。実データ1件を共有いただければ調整します |
| 入金照合が「要確認」 | 手数料・返金の扱いを実データに合わせて調整が必要。ご連絡ください |

困ったら、エラーメッセージのスクリーンショットを送ってください。
